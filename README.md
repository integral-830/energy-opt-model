Project: Energy Optimization Model (AI + MPC + Storage + DC-Flow)

Setup:
pip install -r requirements.txt
python main.py

Use Dockerfile provided in original instructions to containerize.

To use real 5-year weather and IEX data, set use_real_data=True in main.py and provide CSV paths.